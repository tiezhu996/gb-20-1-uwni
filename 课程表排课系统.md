## 项目主要需求

### 项目概述

构建一套面向中小学和高校的智能课程表排课系统，帮助教务人员高效完成学期排课工作。系统支持手动排课和自动排课两种模式，能够自动检测并避免时间冲突（教师冲突、教室冲突、班级冲突），生成可视化课表供师生查看。

### 功能需求

1. **基础数据管理**：维护学校的基础信息——教室管理（教室名称、容量、类型：普通教室/实验室/多媒体教室）、教师管理（姓名、所属学科、可用时间段）、班级管理（年级、班级名称、人数）、课程管理（课程名称、课时要求、适用教室类型）
2. **自动排课**：根据班级课表需求、教师可用时间、教室容量和类型等约束条件，自动计算最优排课方案；支持设置排课优先级（如主科优先安排上午时段）；排课结果支持一键重新生成
3. **手动排课与调整**：教务人员可通过拖拽课表的方式手动调整排课结果，调整时系统实时检测冲突并给出提示（红色标记冲突项），支持锁定某门课程不参与自动重排
4. **冲突检测引擎**：自动检测并提示三类冲突——同一教师在同一时间段被安排多门课程、同一教室在同一时间段被安排多门课程、同一班级在同一时间段被安排多门课程
5. **课表查看与导出**：分别提供班级课表、教师课表和教室课表三种视角，支持按周查看和按学期查看，课表可导出为 PDF 和图片格式
6. **调课与代课管理**：支持教师发起临时调课申请（交换两个时间段的课程），支持管理员为请假教师安排代课教师，调课和代课操作自动同步更新相关课表
7. **学期管理**：支持创建和切换学期，每个学期可独立配置上课时间段（如上午四节、下午三节、晚自习两节）、每周上课天数和节假日安排

### 技术要求

- 前端：Angular 17 框架，使用 Material UI 组件库，Webpack 构建工具
- 后端：Django 4.x 框架，Python 3.11 开发
- 数据库：PostgreSQL，使用 Django ORM
- 排课算法：基于约束满足问题（CSP）的排课算法，使用 Python 实现
- 认证方式：JWT + Django REST Framework 权限控制
- 课表渲染：前端使用 FullCalendar 组件或自定义课表网格组件
- 导出功能：ReportLab 生成 PDF 课表，前端使用 html2canvas 导出课表图片

## 代码存放规则

这是前后端分离项目。

前端代码放在 `frontend/`：

页面，组件，路由,状态管理,API 请求,类型定义,样式和资源等放在对应的子文件夹

后端代码放在 `backend/`：

数据库脚本放在 `database/`。

## 部署要求

项目必须支持 Docker Compose 一键部署。

需要提供以下文件：

- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- `frontend/Dockerfile`：前端项目构建与运行镜像（多阶段构建，先构建再用 Nginx 托管）
- `backend/Dockerfile`：后端项目构建与运行镜像
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `.env.example`：所有环境变量的示例文件，附注释说明

docker-compose.yml 要求：

- 所有服务使用命名容器，便于管理
- 数据库数据使用命名卷持久化
- 前端通过 Nginx 反向代理后端 API
- 服务间通过 Docker 内部网络通信
- 数据库服务配置 healthcheck，后端服务依赖数据库健康检查通过后再启动
- 暴露端口（避免与其他项目冲突）：前端 8004，后端 3004，PostgreSQL 5502
- 支持通过 `docker compose up -d` 一条命令启动全部服务

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：

- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令）
- 访问地址
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：

- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示
- 不要保留脚手架默认 README 内容

